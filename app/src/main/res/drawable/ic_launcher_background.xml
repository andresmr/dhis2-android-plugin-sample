<?xml version="1.0" encoding="utf-8"?>
<!--
  The harness launcher's background: the Capture App's own field gradient.

  #ADCEFF to #8DFFD7, vertical, lifted from the host's ic_launcher_foreground.xml rather than
  matched by eye. The harness is dev-only and never shipped, but it sits on the same home screen as
  the app it pretends to be, and an icon that is nearly the brand is worse than one that is exactly
  it — the foreground is where this identifies itself as something else.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:aapt="http://schemas.android.com/aapt"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <path android:pathData="M0,0h108v108h-108z">
        <aapt:attr name="android:fillColor">
            <gradient
                android:startX="54"
                android:startY="0"
                android:endX="54"
                android:endY="108"
                android:type="linear">
                <item android:offset="0" android:color="#FFADCEFF" />
                <item android:offset="1" android:color="#FF8DFFD7" />
            </gradient>
        </aapt:attr>
    </path>
</vector>
